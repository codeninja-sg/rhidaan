import tkinter as tk
root= tk.Tk()
root.title("Buttons & display") 
tk.Label(...)
root.mainloop()
title_label= tk.label(root, text="hello, tiknter!"font=("arial, 18"))
title_label.pack(pady=10)
tk.label(...)
.pack()places it in the window
pady=10
name_label + tk.label(root, text+"amaya")
name_label .pack()

name_entry + tk.entry(root, width+25)
name__entry.pack(pady+5)
name = name_entry.get().strip()
if name:
    title_label.config(text=f"hello (amaya)!")
else
    title_label
